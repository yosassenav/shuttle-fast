/*Operators API*/
import type { NextApiRequest, NextApiResponse } from "next";
/*import { UserController } from "@/api/user/controller";*/

/*El segundo import está comentado porque necesitas los archivos: controller.ts, repository.ts y service.ts
que vienen en la carpeta src/api/users/  pero en este caso debes hacer los mismos archivos pero para los operadores (operator) */